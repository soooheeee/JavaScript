const obj = {
    x:1,
    y:2,
    z:3,
};
let key;

for (key in obj) {
    console.log(key);
}

const name = "이도혜";
const country = 'KR';

const user = {
    name:name,
    country:country,
}

console.log(user);

